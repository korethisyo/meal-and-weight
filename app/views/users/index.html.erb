<h1>ユーザー一覧</h1>

<% @users.each do |user| %>
  <%= link_to user_path(user.id) do %>
    <%= attachment_image_tag user, :user_image, :fill, 70, 70, fallback:"no_image.jpg" %>
    <%= user.nick_name %>
  <% end %>
<% end %>
